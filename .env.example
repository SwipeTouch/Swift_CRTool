# Copy to .env: cp .env.example .env
# Local MySQL via Docker Compose (docker compose up -d)
DATABASE_URL="mysql://crms:crms_dev@localhost:3307/crms"

NODE_ENV="development"
API_PORT=3002
WEB_PORT=3000
API_URL="http://localhost:3002"
WEB_URL="http://localhost:3000"

JWT_ACCESS_SECRET="dev-access-secret-min-32-characters-long"
JWT_ACCESS_EXPIRY="8h"

CORS_ORIGINS="http://localhost:3000,http://localhost:3001"

EMAIL_PROVIDER="console"
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM_EMAIL="noreply@swipetouch.local"
SMTP_FROM_NAME="Swipetouch CRMS"

LOG_LEVEL="debug"
