# Copy to .env: cp .env.example .env
# MySQL connection: mysql://USER:PASSWORD@HOST:PORT/DATABASE
# NOTE: Special characters in password must be URL-encoded (& = %26, * = %2A)
DATABASE_URL="mysql://admin_crms:EAeLoz31%26%2Afvfw7c@localhost:3306/admin_crms"

NODE_ENV="production"
API_PORT=3002
WEB_PORT=3000
API_URL="https://changerequest.swiftcampus.com"
WEB_URL="https://changerequest.swiftcampus.com"

# Generate secure secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_ACCESS_SECRET="CHANGE-THIS-TO-RANDOM-64-CHAR-STRING"
JWT_ACCESS_EXPIRY="8h"

CORS_ORIGINS="https://changerequest.swiftcampus.com"

EMAIL_PROVIDER="console"
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM_EMAIL="noreply@swipetouch.local"
SMTP_FROM_NAME="Swipetouch CRMS"

LOG_LEVEL="debug"
